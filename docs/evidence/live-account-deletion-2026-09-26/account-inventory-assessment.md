# Current twelve-component account deletion inventory assessment

Read-only assessment, September 26. This updates the historical `docs/ACCOUNT-DATA-INVENTORY.md`; it does not approve/write ACCOUNT_DATA_INVENTORY or activate deletion. Scope is the current Dev resources and the next explicitly allowlisted disposable Dev account, preserving other accounts. New HTTP-guard installation must be reflected in final source bindings and a repeated cutover audit before approval.

## What is actually established now

All thirteen declared stores exist and are ACTIVE. All 33 observed application/API log groups have explicit14-day retention, including post-confirmation. Recovery storage now exists with7-day PITR. History stores have7-day PITR; six foundation stores and campaign intelligence have35-day PITR; campaign pipeline/outbox and Play token storage have PITR disabled. Table TTL configuration is not proof of item coverage or timely physical erasure.

The two profile writers run c4b1cae on Python3.14 with transactional profile mutations and ledger ConditionCheck permission. The six-stage rollout, exact readback, real isolated policy qualification and installed policy simulation are durable evidence. The old baseline statements that profiles/log retention are unimplemented are obsolete. These checks do not claim an actual current Dev signup flow has been exercised.

Campaign completion f922 and account-data997e recovery-required guard are deployed closed. The actual48 campaign and52 all-component fixtures exercise real receipt producers/transactions. Two subsequent fresh disposable AWS Cognito pools exercised actual identity removal and committed-delete response loss/retry with all eleven real upstream receipts; cleanup confirmed pools/functions/roles/tables absent, while temporary KMS keys are PendingDeletion, not destroyed. Synthetic inventories, purchase/cipher fixtures and isolated runtime IAM remain explicit limitations. A session was not minted, so GlobalSignOut invocation alone does not prove rejection of an already issued JWT.

History's terminal-safe bridge/lifecycle997e packages and tightened receipt permissions are installed, gates off, with actual role projection tests and no-drift. V1 authority and Play deletion match the39cce616 source family; gates false, allowlists empty. The retained HMAC inventory passes its production validator and current/available secret versions match exactly. Available version metadata cannot reconstruct a removed historical secret; bind original key creation/initialization evidence and current namespace classification rather than invent another rotation requirement.

## Component disposition

| Component | Current proof and retained scope | Remaining deployment/inventory action |
|---|---|---|
| SESSION_REVOCATION | Actual all-component Cognito wrapper calls GlobalSignOut, real receipt producer and finalizer; command remains original through retries.120-day minimal receipt. | Bind real Dev pool/subject mapping and runtime role; exercise fresh authenticated scoped Dev admission and post-fence rejection. No device/paid/trial prerequisite. |
| DEVICE_BINDINGS | Real producer fixtures cover102 devices, authoritative pointer, pagination and repeat safety. | Current full scans recognize both binding rows; no pointer exists but whole-partition deletion does not require one. Bind deployed registration/recovery writer packages and deletion fences to current tables. |
| DEVICE_RECOVERY | Store now provisioned; real producer preserves approved90-day audit while erasing owned transient controls;7-day receipts/24-hour rate and7-day PITR unchanged. Unknown fixture state blocks completion. | Current recovery table is empty in two full stable strong scans. Bind current recovery writer fence/source. No new retention approval needed. |
| ANALYSIS_ABUSE | Real producer/History paths cover request/consumption/rate families and preserved dedup; legacy analysis is mandatory read-only and no longer dispatches new provider work. | Current abuse store is empty in two full stable strong scans. Preserve source/schema coverage; do not reset usage or treat expiry metadata as physical deletion. |
| HISTORY | Both current attached History stores are empty in complete bounded scans; real fixtures cover3x26 records/generations, locators, job lost-ack and terminal replay. Deployed terminal-safe producers/IAM qualified. | Reconfirm empty stores under bound writer state, derive lifecycle start hour from observation/current retained state, enable cleanup-only bridge/lifecycle with source-selected alarms. No new broad fixture rerun needed. |
| CAMPAIGN | Full post-boundary pipeline/outbox/intelligence scans and writer/role checks qualify current-resource absence; only exact1478/1479 registry metadata remains. f922 actual producer/publication/receipt proofs passed. | New HTTP source install changes account-data writer binding; repeat final audit, approve scoped manifests/restore quarantine, bootstrap current1480 key/generation and select deletion-only worker controls. No whole-period retirement or lost1478-key reconstruction required for independently absent data. |
| CAMPAIGN_OUTBOX | Empty current outbox; current producer and cleanup contracts have owned locators/fences. Retired analysis cannot emit despite retained identity-policy Allow; account-data cleanup is closed. | Bind final writer source+role and preserve closed research producers; include late event/stream restore rejection. |
| ENTITLEMENTS | Ownership marker exists, strict validator passes, current token table empty. Real ownership lock/locator cleanup and paid reservation preservation fixture passed; legacy purchase writer retired/denied. | Full in-memory classification now resolves all six purchase-entitlements rows: two recognized legacy rows, exact ownership/HMAC markers and two exact old null-cursor controls. Preserve legacy usage; no migration/reset or historical allowance inference. |
| V1_AUTHORITY | Exact live deletion package/alias matches reviewed source. HMAC marker matches retained key material and namespace IDs; real fixture preserves global7 used while releasing1 reserved on account cleanup. | Bind original key lifecycle evidence; both old non-USER sweep controls are exact four-field rows with null cursors. Choose only new verified subject in explicit allowlist, enable deletion-only mapping/reconciliation. Global minimized purchase-period usage is approved retained data, not an account-owned row to erase/reset. |
| PLAY_TOKENS | Dedicated table empty, no PITR, strict paired encrypted-token/reverse mapping/delete fixtures passed, real producer receipts included. Dedicated deletion runtime has no token decrypt/provider requirement. | Rebind current no-backup/resource policy evidence and exact role/table/HMAC; enable deletion-only worker/allowlist. No five-minute lifecycle checkpoint approval or provider activation needed. |
| USER_PROFILE | Transactionally fenced deployed profile writers; real fixtures remove owned profile only after required progress, retaining only approved audit/evidence. | Fresh Dev identity audit verifies Username=sub and owned profile; one existing administratively created identity without PROFILE remains excluded/untouched. Fresh PENDING_AGE_GATE profile is admissible; do not fake age verification. |
| IDENTITY | Two actual isolated AWS cases confirm real AdminDeleteUser and absence reconciliation after a lost response, exact receipt/fence completion, no repeated deletion. | Actual Dev pool grant/identity mapping, full exact12-component manifest and SEALED/pendingJobs0 proof must be selected; then test one newly created Dev identity via actual JWT route. Isolated pool results do not prove the app route or existing token invalidation. |

## Remaining concrete coverage work

1. **Current record-family classification is complete for the observed rows.** The five-store full-read audit now recognizes all eight users rows (two profiles, two participation states, two operations, two400-day consent audits) and both device rows; recovery, abuse and URL cache are empty. Actual participation validators ran only against in-memory adapters, and audit cleanup validators passed. Both scans per store were stable. Legacy entitlement shapes are recognized and preserved. The final ownership report resolves all six rows with zero unknown families; the independent control observation confirms both old four-field null-cursor rows. Bind these final reports to the closed-writer boundary; shape recognition does not create allowance or retrospectively approve historical copies. Existing synthetic tests need not be repeated.
2. **Root: bind all current writers, not only cleanup workers.** Preserve profile rollout evidence; add/reuse exact installed device registration/recovery, History mutation/read admission, modern authority/Play/research closed gates, and mandatory retired legacy analysis/purchase paths. Relevant old published versions/aliases and their current roles must remain covered. Known campaign writer binding is already complete except the forthcoming account-data source replacement, which intentionally requires a fresh audit.
3. **Root: preserve explicit copy boundaries.** Existing PITR retention and restore quarantine are approved; complete deletion means owned active-store erasure plus disclosed retained evidence, not immediate destruction of backups/logs. Reuse current no-backup Play evidence and existing regional native-backup/export/AWSBackup observations, flagging any actual uncovered copy source. No assertion that metadata can find all unmanaged copies. The owner reports no manual copies/restores. Do not demand a restore exercise or physical test merely to restate these boundaries.
4. **Root: explicit current-resource approval and marker coherence.** Locator/recovery/completion plus12-component account manifest must reference the final audited source/roles/lineage and exact approved policy/identity evidence. All approval epochs must precede the accepted command strictly. Existing ownership and HMAC markers are separately validated controls; no need rewrite them unless their evidence actually changes. Late old commands are not silently requalified.
5. **Root/Lambda: scoped workers-first rollout.** New HTTP guard uses empty subjects in workers mode and1..10 canonical subjects in API mode. Keep API routes withheld during worker activation and keep unrelated providers/research/export closed. Prove cleanup mapping/schedule/IAM monitoring before accepting a24-hour deletion obligation. Execute actual signup/auth/fresh POST and observe real receipt progression/identity absence on one expressly new disposable Dev account. This is the remaining deployed acceptance, not a reason to re-run all completed isolated suites.

## Shared/non-component stores and copies

The URL-risk cache is shared by URL hash, without an account ownership index. Baseline raw-URI writes were removed and legacy endpoint is retired; the current full-read classification now confirms the attached cache is empty in two strong scans. This does not inspect past backups. Any future observed old cache payload remains shared retained data with its existing logical-expiry/backup policy, not a user-owned row a generic account query can find. For the newly created subject with provider paths kept disabled, no new cache contribution should be fabricated as a prerequisite. External provider, store, mobile export/local files and logs remain separately disclosed copies; this twelve-component server marker cannot claim their erasure. Android's approved accepted-request local cleanup is separate and is not a blocker to backend qualification.

## Evidence inputs

- `/tmp/amt-account-family-classification.json`: full in-memory classification of five stores, two stable strong scans each; all10observed rows recognized, no raw values retained.
- `/tmp/amt-legacy-entitlement-shapes.json`: both preserved legacy entitlement rows recognized, no usage reset.
- `/tmp/amt-all-account-storage-readiness.json`:13 stores/33 finite logs; metadata-only.
- `/tmp/amt-authority-inventory-readiness.json`: retained HMAC/current marker, live V1/Play aliases and projected namespaces.
- `/tmp/amt-ownership-history-readiness.json`: ownership marker, all six recognized entitlement/control rows and empty History; no remaining unknown families in that bounded read.
- `/tmp/amt-campaign-inventory-candidate.json` and `/tmp/amt-ledger-row-classification.json`: current attached campaign absence plus resolved cursor, subject to final source refresh.
- Root repo `docs/evidence/profile-writer-fence-2026-09-23/`, `docs/evidence/live-account-deletion-2026-09-26/`: actual installed profile/History/campaign/authority evidence, actual isolated IAM/runtime/Cognito outcomes and honest cleanup limits.

No source files, AWS records, keys, schedules, routes or approval markers were changed for this assessment.
